#include <stdio.h>
int main()
{
	float a,b,c;
	printf("Nhap so a:");
	scanf("%f",&a);
	printf("Nhap so b:");
	scanf("%f",&b);
	printf("Nhap so c:");
	scanf("%f",&c);
	if (a==0);
		printf ("Phuong trinh tro thanh B1");
	else
		delta=b*b-4*a*c;
		if (delta<0)
	
}
